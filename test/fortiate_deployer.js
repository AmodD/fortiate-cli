'use strict';
// var expect = require('chai').expect;
// const shell = require('shelljs');
// const logSymbols = require('log-symbols');
// let deployer = require('../deploy');

describe('Deployer Command', function() {

  it('shows a warning if deployer is not installed', function() {
  //  const incorrectDeployerPath = '/usr/local/lib/node_modules/deployer123';
  //  var result = deployer.cdToPath(shell, logSymbols, incorrectDeployerPath);
  //  expect(result).to.have.string('Deployer does not exist!');
  });

  it('node deploy is installed globally', function() {
    // const globalDeployerPath = '/usr/local/lib/node_modules/deployer';
    // var result = deployer.cdToPath(shell, logSymbols, globalDeployerPath);
    // expect(result).to.not.have.string('Deployer does not exist!');
  });

});

